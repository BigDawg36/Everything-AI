# Python bytecode caches, created whenever a skill's scripts are run in-place
__pycache__/
*.py[cod]

# macOS
.DS_Store

# npx skills add installation cache (symlinks into .claude/skills/ are tracked; this dir is not)
.agents/
